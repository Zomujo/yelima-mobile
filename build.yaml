targets:
  $default:
    builders:
      drift_dev:
        generate_for:
          - lib/core/db/**.dart

      json_serializable:
        options:
          explicit_to_json: true
          include_if_null: false
